<div class="w-full border-b">
    {% if taxons|length > 0 %}
        <nav class="py-4">
            <div class="container">
                <div class="flex flex-col md:flex-row gap-6 md:items-center md:justify-center" {{ sylius_test_html_attribute('menu') }}>
                    {% hook 'sylius_shop.base.header.navbar.menu' with { taxons } %}
                </div>
            </div>
        </nav>
    {% endif %}
</div>
